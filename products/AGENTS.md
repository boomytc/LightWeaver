# Products

Each `products/<name>/` is a standalone product root.

- Import `@lightweaver/weaver`. Do not import sibling products.
- Capture may HTTP the LightUI lab. Publish may copy into `LIGHTUI_ROOT`.
- Runtime films: `data/first-party/` (customer jobs) and `data/projects/`
  (user). Both gitignored. Do not commit task instances.

| Product | Role |
| --- | --- |
| `study-films` | Remotion renderer + LightUI lab capture |
| `studio` | Local control site: voices, materials, film assignment |
