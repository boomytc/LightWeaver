# Studio

本地工作台：项目、资产区、场景旁白、校验 / 合成 / 渲染。

```bash
# 仓库根
make install
make studio
```

浏览器打开 `http://127.0.0.1:5175/`。API 在 `127.0.0.1:8788`。

- 内置项目来自 `products/study-films/projects/`
- 新建项目写到 `data/projects/`
- 共享音色 / 元素 / 参考在 `library/`
