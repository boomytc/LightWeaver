成片方法以 `library/assets.json` 里 `kind: method` 为准。Studio `/methods` 或 `weaver method list` 看现网。不要把本文件当目录。

场次卡（apply 内部用，不进方法库）：

kind-still — 往对照表片加/绑一场。
contrast-pair — 两场相邻 still 是易混对，口播里互相指认。
study-title — 任意 study-explainer 片头。
say-it-this-way — 任意 study-explainer 片尾。
